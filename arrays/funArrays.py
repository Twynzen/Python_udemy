# funciones de arrays
import numpy as  np

array = np.arange(5)

#Esto devuleve la raíz cuadrada de cada uno de los elementos
np.sqrt(array)

#esto genera numeros aletorios
np.random.rand(5)

lista = [5,6,7,8,9]

array2 = np.array(lista)

#Esto permite sumar los arrays
np.add(array, array2)

#para calcular el maximo valor de lso arrays
np.maximum(array, array2)
